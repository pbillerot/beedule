# Portail Serveur de répertoires
http:
  routers:
    nginx:
      entryPoints: 
      - websecure
      rule: "Host(`pbillerot.freeboxos.fr`) && PathPrefix(`/`)"
      service: nginx

  services:
    nginx:
      loadBalancer:
        servers:
        - url: "http://172.17.0.1:7603/"
