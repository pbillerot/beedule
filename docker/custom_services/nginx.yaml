http:
  routers:
    nginx:
      entryPoints: 
      - websecure
      rule: "Host(`pbillerot.freeboxos.fr`) && PathPrefix(`/`)"
      # rule: "Host(`pbillerot.freeboxos.fr`)"
      service: nginx

  services:
    nginx:
      loadBalancer:
        servers:
        - url: "http://192.168.1.76:7603/"
