# Portail Serveur de répertoires
http:
  routers:
    nginx:
      entryPoints: 
      - websecure
      # tous les flux vont sur nginx
      rule: "Host(`pbillerot.freeboxos.fr`) || Host(`pbillerot.freeboxos.fr`) && PathPrefix(`/`)"
      # rule: "Host(`pbillerot.freeboxos.fr`) && PathPrefix(`/`)"
      service: nginx
      priority: 2
      # middlewares:
      # - bee-secure@file

  services:
    nginx:
      loadBalancer:
        servers:
        - url: "http://172.17.0.1:7603/"
