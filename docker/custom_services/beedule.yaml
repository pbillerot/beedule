http:
  routers:
    beedule:
      entryPoints: 
      - websecure
      rule: "Host(`pbillerot.freeboxos.fr`) && PathPrefix(`/bee`)"
      service: beedule
      middlewares:
      - secHeaders@file
    
    # rout-auth:
    #   entryPoints: 
    #   - web
    #   rule: "Host(`192.168.1.76`) && PathPrefix(`/traefik`)"
    #   service: beedule

  services:
    beedule:
      loadBalancer:
        servers:
        - url: "http://172.17.0.1:7601/"

