http:
  routers:
    filebrowser:
      entryPoints: 
      - websecure
      rule: "Host(`pbillerot.freeboxos.fr`) && PathPrefix(`/fb`)"
      service: filebrowser

  services:
    filebrowser:
      loadBalancer:
        servers:
        - url: "http://172.17.0.1:7602/"
